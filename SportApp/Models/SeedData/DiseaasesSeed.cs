using Microsoft.EntityFrameworkCore;

namespace SportApp.Models.SeedData
{
    public static class DiseaasesSeed
    {
        //public static void EnsurePopulated(IApplicationBuilder app)
        //{
        //    MyContext context = app.ApplicationServices
        //    .CreateScope().ServiceProvider.GetRequiredService<MyContext>();


        //    if (context.Database.GetPendingMigrations().Any())
        //    {
        //        context.Database.Migrate();
        //    }
        //    if (!context.Diseases.Any())
        //    {
        //        context.Diseases.AddRange(
        //            new Diseases
        //            {
                        
        //            }
        //            );
        //    }
        //}
    }
}

